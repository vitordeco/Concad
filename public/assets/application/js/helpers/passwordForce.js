$( function(){	//iniciar	passwordForceInit();		//atualizar	$("#password-input").keyup(passwordForceChange);});function passwordForceInit(){	//definir target	var passwordForceTarget = $("#password-force");		//definir HTML	var html = $("<li></li><li></li><li></li><li></li><li></li>");	passwordForceTarget.html(html);		//definir CSS	passwordForceTarget.css({		"display": "table",		"width": "540px",		"padding": "0",		"margin": "10px 0"	});	passwordForceTarget.find("li").css({		"list-style": "none",		"float": "left",		"margin-right": "1%",		"width": "11%",		"height": "5px",		"background-color": "#ddd"	});}function passwordForceChange(){	//definir string	var value = $("#password-input").val(); 	//definir força da senha	var force = 0;		//verificar quantidade de caracteres	if( value.length >= 6 ) 	{		force++;	    console.log('verificar quantidade de caracteres');	}		//verificar letras minúsculas	if( value.match(/[a-z]/) ) 	{		force++;	    console.log('verificar letras minúsculas');	}		//verificar letras maiúsculas	if( value.match(/[A-Z]/) ) 	{		force++;	    console.log('verificar letras maiúsculas');	}		//verificar números	if( value.match(/[0-9]/) ) 	{		force++;	    console.log('verificar números');	}		//verificar caracteres especiais	if( value.match(/[!@#$%&*()]/) ) 	{		force++;	    console.log('verificar caracteres especiais');	}	passwordForceTarget(force);}function passwordForceTarget(force){	//definir target	var passwordForceTarget = $("#password-force");		//loop nas li	passwordForceTarget.find("li").css("background-color","#ddd");	passwordForceTarget.find("li").each( function(i, e){				var position = (i+1);				if( (force >= position) && (position == 1) )		{			$(e).css("background-color","#c91a1a");		}				if( (force >= position) && (position == 2) )		{			$(e).css("background-color","#c9881b");		}				if( (force >= position) && (position == 3) )		{			$(e).css("background-color","#c9c01c");		}				if( (force >= position) && (position == 4) )		{			$(e).css("background-color","#63c91d");		}				if( (force >= position) && (position == 5) )		{			$(e).css("background-color","#64c91e");		}			});}