/** * show successs message * @param str */function message(str){	message(str, 'danger');}/** * show message * @param str * @param type */function message(str, type){	$("#message").remove();	var target = $("body");	var html = messageHTML(str, type);	target.prepend(html);	//messageFixedToogle();}/** * message HTML * @param str * @param type */function messageHTML(str, type){	var div = $("<div>")	.addClass("fixed alert alert-" + type)	.attr("id", "message");		var a = $("<a>")	.addClass("fa fa-times close")	.attr("href", "javascript:;")	.attr("onclick", "$(this).parents('#message').slideUp('slow');");		var html = div.append(a).append(str);		return html;}/** * message fixed */function messageFixedToogle(){	// var top = $('body').scrollTop();	// var content = $("div#message");	//	// if( top > 217 ){	// 	content.addClass('fixed');	// } else {	// 	content.removeClass('fixed');	// }}/** * onload */$( function(){		//ao rolar a página fixar os alertas	// $('body').scroll(messageFixedToogle);	});